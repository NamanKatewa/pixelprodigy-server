{
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:dbpush": "prisma db push",
    "start": "nodemon index.js",
    "devStart": "nodemon index.js",
    "dev": "yarn prisma:generate && yarn prisma:dbpush && yarn devStart",
    "build": " yarn install && yarn prisma:generate && yarn prisma:dbpush && nodemon index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "nodemon": "^3.1.2",
    "prisma": "^5.14.0",
    "stripe": "^15.8.0"
  }
}
